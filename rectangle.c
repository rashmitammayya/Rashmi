#include<stdio.h>
void main()
{
int length,breath,peremeter,area;
printf("enter the value of length and breath is",length,breath);
scanf(" %d %d",&length,&breath);
peremeter=2*length+breath;
printf("peremetere=%d",peremeter);
area=length*breath;
printf("area=%d",area);
}

